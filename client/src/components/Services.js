import React from 'react';
import ScrollableAnchor from 'react-scrollable-anchor';

const Services = () => (
  <ScrollableAnchor id={'services'}>
    <div style={{ background: 'rebeccapurple', height: '100vh' }}>Services</div>
  </ScrollableAnchor>
);

export default Services;
