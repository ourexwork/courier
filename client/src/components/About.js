import React from 'react';
import ScrollableAnchor from 'react-scrollable-anchor';

const About = () => (
  <ScrollableAnchor id={'about'}>
    <div style={{ background: 'red', height: '100vh' }}>dlkdldl</div>
  </ScrollableAnchor>
);

export default About;
